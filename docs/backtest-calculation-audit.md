# 回测计算口径与第一批修复记录

2026-09-24。第一批已完成研究分层路径修复；不代表整套回测系统已经完成统一。

| 入口 | 当前收益定义 | 成本与时间 | 本批状态 |
| --- | --- | --- | --- |
| 因子详情 | 20日非重叠等权分层，顶组减底组 | 同日收盘至20交易日后收盘；默认不扣费 | 已接共享research_backtest |
| 在线因子实验室、因子库 | 5日非重叠等权分层，顶组减底组 | 同日收盘至5交易日后收盘；默认不扣费 | 已接同一入口，修复5日误标20日 |
| static_backtest | 固定权重选Top-N，前向收益减池内均值 | 换手比例×成本；样本内诊断 | 尚未统一执行账本；需继续审查重叠参数与取样日历 |
| walk_forward | 历史IC估权，Top-N前向收益减池内均值 | 已拒绝step小于持有期；换手比例×成本 | 仍是前向窗口评估，缺完整执行账本；缺失窗口、现金间隔与月度统计待统一 |
| strategy_backtest | 固定策略权重前向持有期收益 | 换手比例×成本，简化调仓 | 本批不改变；完整成交与每日账本安排在阶段三 |
| Qlib回测浏览 | 读取已有组合/基准/费用产物 | 按产物逐日复利 | 外部实验产物，不能仅凭页面推定与本地策略同口径 |

## 已修改

- 新增纯计算模块research_backtest：拒绝重叠持有期；以排序后的行情交易日期取样，到期日记录收益；组内使用均值而非中位数，分组不重叠，同值按证券标识稳定排序。
- 先按当日因子分组，再检查已分组股票起止价格；价格缺失不事后剔除换股。缺少截面/价格的窗口标记incomplete，保留诊断并停止提供净值和绩效，不能压缩时间粉饰结果。
- 理论多空为每单位本金多空各1倍，默认费用为0；可显式指定每腿往返费用，两腿均扣除。它是收盘价研究结果，不是可执行交易回测。
- 净值包含初始本金1，年化使用实际覆盖交易日数；回撤包含首期损失。零方差、单期不返回虚构夏普；取样步长大于持有期时不报告夏普。收益损失达到本金时停止复利。
- 详情页删除旧的重叠20日收益逐日复利；滚动IC更名，新增1/5/10/20日前向IC数值诊断及有效天数。未开展新图形建设。
- 实验室、因子库拒绝显示旧规则会话缓存；统计页默认按当前因子定义版本、评估规则与valid状态筛选；详情页不展示未满足该条件的评分摘要。

## 验证

临时数据库内执行45项unittest（新增研究回测/证据筛选/共享入口13项，既有评估完整性、队列、策略完整性与交易硬闸32项），全部通过。原factor_eval的13项函数测试全部通过，合计58项。另有7个修改模块语法检查和git diff --check通过。

手算场景包括两期-10%/+20%得到净值1→0.9→1.08、首期回撤-10%、跨年交易日年化、平价双腿成本、重叠20日标签拒绝、缺失行情与信号、均值/中位数区分、乱序同值分组、未来价格扰动和亏损耗尽本金。

未重启生产服务、未触发真实全量评估、未改持久化历史评分或交易批准。尚未进行浏览器视觉验收；长驻Python进程模块是否加载新版本需后续部署验收，源码落盘不等于线上已切换。

## 下一步

研究分层仍有严格数据覆盖限制，因子预热期会导致报告不完整；后续应显式定义研究起始日和覆盖门槛，不能静默忽略缺失窗口。理论期末回撤不包含持有期内最大回撤。

第一阶段剩余：其他回测入口的收益日历和基础指标统一、费用模型与基准定义逐项对齐。随后排查调度漏跑并做小批量真实验收；阶段三再建立可核账的成交/现金/每日估值账本。历史数据时点不足仍应保留研究限制。


## 第二批：收益日历与窗口绩效修复（2026-09-24）

- forward_returns按日期排序；walk-forward、静态回测、交叉验证、贪心与采样组合的分割索引统一基于行情日历，不再以因子日期交集缩短时间轴。
- 静态回测拒绝重叠持有期和无效参数，仅处理已成熟标签。调仓因子缺失或股票不足时明确失败，不偷偷移动调仓日。选中股票无穷收益同样拒绝。
- walk-forward与策略回测接入共享窗口绩效：按首次调仓至最后到期的真实交易日数年化，回撤包含首期亏损；窗口不连续、间隔持有或方差为零时不提供有效夏普。
- 月份按收益到期月份归集并复利，不再把每22条记录或固定若干持有期当作自然月。输出monthly_basis明确这不是每日估值的自然月绩效。
- 策略净值日期改为到期日；输出完整净值与选股日志，不再仅保留前5次、前3只。移除短期年化强行使用至少0.1年的隐藏下限。
- 空仓间隔后按重新建仓计换手成本，包括walk-forward中因训练不足跳过的窗口。沿用的名单替换比例成本模型仍只是研究近似，不等于资金权重换手或实际成交账本。

验证：52项unittest（含新增7项日历/策略结果测试）通过，原factor_eval 13项函数测试通过；4个修改模块语法检查通过。未重启生产服务、未回写历史结果、未触发全量回测。

剩余：基准当前仍使用可用前向收益的池内均值，需改为时点可用股票池及缺失行情显式处理；窗口回撤仍非每日盯市回撤。报告使用的新窗口口径需接入持久化版本迁移和前端标签，其他页面可能仍沿用“月度胜率”的旧名称。完整交易账本和队列运行根因尚待后续批次完成。


## 第三批：基准缺失处理与报告标识（2026-09-24）

- 新增benchmark_returns：仅用起始日有效正价格确定面板内等权基准成员，到期缺失/非有限/无效收益令报告失败，禁止按未来行情是否完整事后剔除。静态、walk-forward、策略回测共同使用。
- 新增research-window-v3计算标识和统一报告契约，明确基准为提供面板中的起始日有效股票、非历史成分股认证；月度指标为到期月归集、回撤为持有期末序列、费用仍为名单替换近似。静态/滚动DataFrame attrs、策略结果及定期重验持久化JSON携带这些字段。
- 定期重验不再私自重算旧版夏普，改用共享结果。现有持有5日、步长10日配置的夏普不可用，不能通过要求夏普的晋级条件；保留原参数，不用更改频率掩盖证据不足。
- 夏普不可用不再误称“夏普为负”；新版可信度明细使用“到期月归集胜率”。
- Walk-Forward日志页按run_id选择实验，停止拼接不同实验；现有日志结构缺少完整到期窗口和计算版本，暂时停用累计净值、保留原始记录。月份分组明确标注按信号月统计窗口均值，非自然月收益。

验证：59项unittest通过，原factor_eval 13项函数测试通过，共72项。新增7项基准/契约测试覆盖未来缺失、起始不可用、非选中基准股票缺失、静态及滚动契约、滚动基准缺失、缺失夏普诊断。语法和diff检查通过。未进行真实定期重验或浏览器验收，未修改生产数据库或重启服务。

剩余：尚无历史成分股时点认证；新标识不自动使历史审批失效，也不等于新审批机制已完成迁移。旧日志到新报告存储的接通、失败重验的持久化证据、调度队列实际推进及完整交易账本继续待办。


## 第四批：成功/失败报告持久化（2026-09-24）

- 新增按report_id追加的strategy_validation_reports历史表；同日多次重验分别保留。旧当日摘要在首次覆盖前归档，原strategy_validation继续作为每日最新摘要。
- 报告写入、当日摘要与策略状态/胜率更新在同一事务内；失败写入回滚，避免只改状态未保存证据。NaN/Infinity递归转null，JSON严格序列化。
- 定期重验分离计算与发布：计算异常、因子不足、空窗口均保存失败报告；当前版本失败使策略degraded并清空旧胜率。停用策略提前返回，不启动计算。
- 重验前后比较策略定义；发生版本变化时仅追加stale_version报告，不覆盖每日摘要或策略状态。该检查仍不是跨进程版本CAS，检查到事务之间的极短竞态需后续加强。
- 股票池为空不再回退沪深300；部分因子异常/空值不继续评估残缺策略。成功报告带策略配置快照、逐窗口记录、共享指标及计算契约；尚未保存可重放的完整行情快照。

验证：66项unittest和13项原factor_eval测试通过，共79项；新增7项报告测试覆盖重复日期、异常记录、版本漂移、写入回滚、停用策略、旧摘要归档和股票池缺失。语法和diff检查通过。只在临时数据库测试，未手动迁移生产库、重启服务或启动真实重验；新表在新代码首次保存报告时创建。

只读运行核查：2026-09-24 21:31:17定时任务已完成30个因子（有效27、样本不足3、错误0）；队列累计有效29、样本不足4、待处理39,867。该轮来自现有运行任务，不是本批主动触发。18:00漏跑根因仍未确定，不因晚间成功宣称调度问题全部解决。

剩余：报告读取页面与日志接通、严格版本CAS、运行中任务失败恢复、真实小批量/部署验收及成交账本。报告记录本身不提供交易批准，仍沿用现有执行硬闸。


## 第五批：报告读取与事务内版本校验（2026-09-24）

- 新增validation_report_reader，只读SQLite连接读取最近200份报告，不调用会迁移表结构的连接入口。无库/无报告表时返回空，不创建文件或表；损坏JSON作为错误记录展示。
- Walk-Forward页面接入报告选择、失败原因、策略/计算版本、保存的逐窗口明细和JSON导出。与历史日志分开，不重新计算另一套指标，也不把曾发布的结果标成当前有效批准。
- 策略定义加载提取为同连接函数。报告发布在BEGIN IMMEDIATE写事务内重新读取策略及注册表回填定义，再核对expected_version和停用状态；不匹配时只归档，防止检查与写入之间被其他SQLite写入修改定义。
- 新增published字段，明确区分归档与曾发布摘要；保留历史报告和失败原子回滚逻辑。低层保存API仍允许不传expected_version以保存兼容历史数据，生产重验入口显式传入。

验证：69项unittest通过；Streamlit AppTest验证无数据库和失败报告两种页面分支均无异常，空库分支不创建数据库。5个模块语法和diff检查通过。页面验证使用临时数据，不是浏览器视觉验收。

未重启服务、未修改生产数据库或进行真实重验。部署前仍需备份目标表、确认运行任务状态并验证当前新代码导入；首次真实发布后核对历史表、每日摘要和策略状态。报告尚缺可重放行情快照；历史成分股、执行账本和此前漏跑根因仍未解决。


## 第六批：部署与单策略验收（2026-09-24）

- 确认调度心跳新鲜且running为空后，备份strategies 26行、strategy_validation 1行、factor_scorecards 3631行；生产当时尚无历史报告表。备份路径/data/backups/backtest_deploy_20260924_215820.sqlite，integrity_check=ok。宿主备份目录权限不足，改用容器内同目录完成。
- 21:58:40重启lh-qsys加载源码；Streamlit和SSE健康接口正常，调度器启动成功。其余容器未重启。
- 对已有degraded策略“稳健低波组合_v1”执行单次真实重验，180秒外部上限。21:59:10开始，22:02:09超时退出124；未形成完整报告，不能视为真实策略重验验收通过。
- 现场数据路径get_panel在缓存未命中时逐只股票调用_cached_daily；进程等待、资源使用平稳，但没有栈证据，不能断言网络或某个调用就是根因。下一步应增加分阶段计时和本地数据覆盖诊断。
- 将本次部署超时单独以source=deployment_smoke_test、assessment_status=timeout归档，published=false；未验证原策略版本，因此明确original_strategy_version_verified=false，不赋予原始计算证据地位。报告ID fc69839fb42b4ae3bd5d337be67af34f。
- 落库回读：历史报告存在；该策略当日摘要仍0条；状态和原胜率保持degraded/33%，不是本次结果。生产只读报告入口可读取该超时记录。归档触发创建历史报告表；服务两健康接口再次正常。

验收结论：部署和报告归档读取通过；真实完整计算、浏览器视觉和吞吐验收未通过/未完成。不得将旧33%胜率说成此次重验结果。后续优先处理取数/计算耗时观测与超时持久化机制，再重试受限单策略，不扩大到全量。


## 第七批：阶段计时、可归档超时及重复取数修复（2026-09-24）

- validation_trace按run_id写独立JSONL，记录股票池、面板、逐因子、输入HDF、Python子进程、walk-forward、交叉验证和市场状态计算阶段。报告保存阶段事件。
- 独立主线程可传timeout_seconds，使用进程计时器取消计算，恢复原信号处理器；超时异常绕过数据源普通异常重试，归档timeout而不发布摘要或改变策略状态。调度线程默认不启用此计时器，尚未接独立子进程调度；不能宣称所有定时任务已具备超时保障。
- 25秒真实复测：面板0.041秒、vol_20d 0.581秒，超时发生在downside_volatility_20d。报告90eb79dd00d34caf9cc5ad28444d73e7自动归档、published=false。
- 检查Python因子输入构造发现它绕过已加载面板重新调用fetch_panel。非Qlib路径改为复用相同源/窗口的缓存面板，显式整理(datetime,instrument)和常数factor；Qlib保留原调整因子路径。evo缓存键加入lookback和input-v2，隔离历史长度与旧输入版本。
- 修复后40秒上限复测在约16秒完成各计算阶段：输入HDF 0.109秒，Python执行0.459秒，walk-forward 2.863秒，交叉验证3.415秒，市场状态5.755秒。此前慢因子本次总耗时1.304秒；缓存预热可能也影响总耗时，不据此声称固定加速倍数。
- 完整报告c313d8de35b84f39845799d5ce66de33：60个OOS窗口，胜率48.333%，平均扣费超额0.124657%，期末超额序列最大回撤-20.0093%，夏普null（间隔持有），策略仍degraded。报告与当日摘要ID一致，策略胜率更新为48%。计算完成不代表验证晋级或交易资格。

验证：71项unittest通过，包括自动超时归档、不改旧状态、真实Python子进程复用面板与不同lookback缓存隔离。语法及diff检查通过。本批新代码由独立验收进程加载，长驻调度器尚未重启加载本批新增计时；上批部署仍运行。

剩余：定时任务独立进程限时执行、逐股票输入覆盖诊断、无缓存首次取数性能及完整交易账本。信号式超时为Unix主线程取消机制，并非操作系统硬实时期限；解释器无法处理中断的原生调用仍可能延迟取消。


## 第八批：定时重验独立进程与重复运行保护（2026-09-24）

- 新增validation_worker：子进程只计算，父进程负责已有版本校验、报告发布与状态事务；单策略默认180秒硬等待上限，超时清理整个计算进程组并归档，不发布残缺结果。正常退出也清理可能残留的因子子进程。
- run_bounded使用每策略跨进程文件锁，重复调用返回busy；定时任务改走该入口，并遵循pool_name筛选，跳过停用/归档策略。直接调用低层revalidate_strategy不受该锁约束，外部入口应使用run_bounded。
- 75项unittest通过，包括进程组超时清理、子进程不发布、同策略锁、调度池过滤；语法和diff通过。
- 真实正常路径：稳健低波组合_v1约13.97秒完成，60窗口，报告6e6958c6765f4ef8989f239d245b51ac发布，仍degraded。
- 刻意0.01秒超时验证：报告b0ed706fe41f4c11940f5b0fbead5664只归档；当日摘要仍指向正常报告，状态degraded/48%不变。此记录是运行机制测试，不是策略绩效失败。
- 重启前备份/data/backups/validation_worker_20260924_222319.sqlite（策略26行、摘要2行、报告5行），完整性ok。确认running为空后重启lh-qsys加载本批与此前计时/面板复用代码；网页/SSE健康接口和调度器启动正常。

边界：该限时范围是子进程计算，父进程预读/持久化仍使用数据库自身超时；批次总预算、父进程崩溃后的孤儿进程监管及无缓存首次取数仍待完善。没有触发全量定时重验，不宣称已覆盖所有策略。交易账本和历史时点数据仍未补齐。


## 第九批：全库覆盖可观测性（2026-09-24）

- 队列新增coverage统计和调度结果尾部摘要，明确total/done/pending/terminal分类及覆盖率。done包含valid、sample_insufficient、data_error、compute_failed，不代表有效因子或交易资格。
- 新增coverage_report，可按当前三个自动窗口批量估算剩余天数；估算只基于处理容量，不把样本不足强行重试，也不保证数据源吞吐。当前快照39,867待处理，若每天70个仅作理论估算约569.5天；实际会受窗口、失败退避、重评和锁影响。
- 本批未改变门槛、股票池或调度频率。覆盖率功能先用于运营观测，避免误报“回测完成”。
- 策略完整执行账本尚未接入strategy_backtest：现有broker具备独立账户/委托/成交/持仓表，但研究回测仍使用前向收益；下一批应建立历史撮合器，不能把broker实时表直接回放成历史成交。

验证：新增覆盖报告测试，相关队列和策略完整性测试共16项通过；语法检查通过。真实快照的报告可由coverage_report.build()生成，未触发全库计算。

## 第十批：历史执行账本接入（2026-09-24）

- 新增 `historical_execution.simulate`：按日开盘全额成交，维护现金、整手持仓、手续费、卖出印花税、订单、成交和权益曲线；缺行情、现金不足、持仓不足均失败关闭。
- `strategy_backtest.backtest_strategy` 增加显式 `mode` 参数。默认 `research` 保持前向收益研究口径；`mode="execution"` 才生成等权整手目标持仓并运行历史执行账本，同时返回 `assessment_kind` 和执行限制。
- 执行账本不模拟盘口排队、部分成交、盘中路径；涨跌停过滤仍由信号生成方负责。它是可核账的日频撮合回放，不等同于实盘成交。

验证：历史执行4项测试、策略完整性10项测试通过，语法和diff检查通过。

## 第十一批：修正执行时序、独立账本绩效与覆盖统计（2026-09-24）

更正第十批结论：当时只是初步接入，不具备真正次日成交；测试实际为历史执行4项与策略完整性6项合计10项，不能作为完整执行验收。原版execution仍使用研究绩效，现已替换。

- execution从因子评分后独立分支，完全不调用forward_returns；收盘决策的目标数量在决策日固定，下一行情交易日开盘执行。按命名索引重排datetime/instrument，截取请求回测区间。
- 等权目标按决策日账户权益和收盘价计算，保留费用预算；卖出优先，开盘跳空导致现金不足时记录拒单、不透支、不部分成交。遗漏的旧持仓目标归零；持有股数、现金流水、费用、成交和每日权益可导出为JSON记录。
- 日频净值、收益、年化、回撤、夏普和月胜率全部来自账户权益；无执行基准时明确标注未实现，不沿用研究基准超额收益。CLI增加--mode execution，默认仍为research。
- 缺失选中股票成交或持仓估值行情时整体失败，不过滤股票。缺失信号日期、末日信号、重复行情、非整数目标等均拒绝。输入有停牌、成交量或涨跌停字段时执行相应限制并记录拒单原因；可卖检查包含买入日期。每日每股至多一个目标，信号不允许同日买卖。
- coverage排除superseded旧版本；覆盖面板接入共享状态组件，读取保存的启用窗口、股票池、批量并按worker的100上限计算容量。配置容量不是实测吞吐，不代表全库完成；未提高生产调度批量。

验证：70项unittest通过（账本、执行集成、策略完整性、覆盖、队列、研究回测、基准、交易日历、报告及worker）；9个变更Python文件语法检查、git diff --check通过。集成测试证明两种索引顺序均可执行、禁止调用前向收益、缺失选中股票开盘价不能生成成功报告、收益与最终权益一致、报告可JSON序列化。

边界：本批未重启服务、未触发全库计算、未完成浏览器视觉验收或真实策略执行报告验收。缺少涨跌停/停牌字段时通过constraints_missing显式暴露，不宣称具备全部交易约束。日历取自行情日期，仍需权威日历完整性检查；历史时点股票池、未复权成交价格与公司行动、历史费率、完整成交量约束、盘口/部分成交、执行报告持久化与执行基准仍未完成。日频执行是研究模拟，不是实盘成交复刻或交易批准。

## 第十三批：策略因子定义补齐与求值错误明细（2026-09-24）

- 回测原先直接读取strategies.factors，跳过list_strategies使用的注册表补齐，因此缺code的演化因子会求值失败。提取共享resolve_strategy_factors，两个入口共同使用；已有代码快照不覆盖、不按当前注册表重新路由。
- 固定本次行情与因子求值的数据源，返回逐因子名称、类型、异常类型和原因；日期明细页显示失败表，仍禁止残缺策略产出成功结果。
- 24项相关回归通过，新增补齐/快照保护测试与完整性回归9项通过（其中6项重复）；diff检查通过。
- 真实单因子downside_volatility_20d补齐后成功返回184096条非空记录。稳健低波组合_v1完整执行复测已越过因子求值，随后因2025-03-21 SH600803的volume无效而失败关闭。该行情问题尚未修复，不能宣称完整回测成功，也未将缺失成交量替换为零或跳过限制。

## 成交量未知的保守诊断回放（2026-09-24）

原始ths_ifind行情中SH600803在2025-03-21及相邻日期volume/amount为NULL，不能据恒定价格推断停牌，也不能填零。本次仅改变诊断回放处理：目标委托遇到未知/非法成交量时拒绝成交，保留原持仓和现金，记录unknown_volume与data_issues；返回data_quality_status=incomplete。ok仅表示模拟流程完成，不能代表数据完整或验收通过。页面显著警告并展示缺失明细，其他必需价格或约束字段错误仍整体失败。

16项相关测试通过（含未知成交量买入/卖出不成交且保留账本，以及页面集成）。真实稳健低波组合_v1回放区间2024-08-30至2026-09-24，783笔成交、3笔拒单、2条数据问题。未修改行情数据库；源数据修复仍未完成。结果仅为保守诊断模拟，不能用于完整数据下绩效结论。
