# 因子挖掘降频（2026-09-24）

已停用loopengine全天15分钟轮动及调度独立事件入口；multitype_mine调整至周一至周五19:30，每日最多一个批次，按十类固定顺序执行，每类最多15个候选。不再按iteration选择类型，不附加事件子轮次。

mining_policy使用文件锁互斥、SQLite记录每日尝试及各类型数据指纹。量价比较训练帧，其他类型比较专属数据帧；数据无效或未变跳过。失败尝试保留当日名额和已预留指纹，防止反复调用；没有新增自动重试。prepared帧复用于同轮计算，避免检查后又取另一份数据。周末不运行；当前按工作日判断，尚未另接法定交易日历。独立RD-Agent容器未调整，其他直接调用引擎的入口不受本调度策略覆盖。

保存的scheduler_state.json已备份至scheduler_state.before_daily_mining_20260924.json并更新。2026-09-24 10:53重启lh-qsys后两项健康接口正常，日志确认loopengine不再启用，multitype_mine启用。此次重启也会加载此前未提交的理论关联和预算修改。

3项新调度策略测试、14项理论链路测试、12项执行资格测试全部通过；语法和diff检查通过。未触发生产挖掘做验证，首个真实盘后批次尚待观察。源码尚未提交或推送。

## 动态配置页面

设置页面新增“因子挖掘频率与轮动”表单：开关、每日1–4批、每批每类1–3轮、每轮每类1–50个候选、首批16:00–23:59、批次间隔1–7小时、无新数据跳过。禁止时间跨日。这里每类轮数指同类型连续运行指定轮数后处理下一类型，不是全天无限循环。

默认仍为19:30、1批、1轮、15个候选。保存一次写入现有scheduler_state，现有心跳检测修改后更新CronTrigger；正在运行任务使用启动时参数，已用每日额度保留，降低上限不会撤销已执行任务。旧batches额度迁移至batch_attempts，失败也占额度。保持共享互斥与交易准入规则，关闭无新数据跳过会允许有意重搜相同数据，增加计算与LLM消耗。

6项挖掘策略、2项调度配置、14项理论、12项执行测试，共34项通过。未用浏览器进行视觉验收。2026-09-24 11:00重启qsys加载新增功能，健康接口正常，实时调度记录下一次为09-24 19:30；以后页面保存参数无需重启。源码仍未提交推送。

## 手动触发一次

设置页面新增“立即手动挖掘一次”按钮；定时任务页面run_now同样提交持久化一次性请求，由调度器owner在60秒心跳内领取。重复点击待处理/运行中请求返回同一编号。手动仅绕过工作日和盘后时段检查，不绕过数据去重、互斥、每日额度或交易审批；占用当天批次额度。

8项挖掘测试、2项配置测试通过。2026-09-24 11:11:31已实际启动手动批次，run参数rotations=2、batch_per_type=15、daily_batches=1、manual=true，请求5006e1cff56d4a268e042440d33351ed。已确认请求和批次均为running，尚未等待整个批次完成。服务已重启加载，源码未提交推送。中途进程崩溃时running请求暂不自动重试，需人工核实，避免重复消耗。

### 2026-09-24 轮动语义修正

轮动次数指完整类型目录的遍历次数：先执行所有类型的第 1 轮，再执行所有类型的第 2 轮。数据指纹仅在每批次首次处理该类型时检查和预留；后续轮动不得因本批次自身写入的指纹被误跳过。每个类型每轮结束写入 round_complete，并记录各类型已完成轮数，页面不得将该状态显示为正在挖掘。

已启动的旧进程仍按旧的逐类型连续多轮顺序执行；本次未中断正在运行的批次。新的执行顺序需调度进程加载更新后生效。
