# 第八轮追加审查（2026-09-24）

保留第七轮未提交改动，继续审查。

## 本轮修复

- broker.get_account增加估值有效性和异常股票信息；require_fresh=True时对非零持仓应用现有10分钟报价时效检查。portfolio_risk实时模式要求估值明确有效，成本价补值不能作为通过风控的依据。空仓不要求行情。
- 板块归属检查完整updated_at时间，拒绝同日未来时刻及无法解析的记录。
- 独立策略回测在调仓日逐一检查正权重因子存在，避免缺一个因子时继续使用剩余因子生成成功报告。

## 验证

容器内selection_gate 12、strategy_backtest_integrity 3、portfolio_risk_integrity 6、buy_atomicity 41、execution_gate 11、validation_hardening 20，共93项通过，进程正常退出。语法及git diff --check通过。测试使用临时数据库/模拟数据，未执行生产交易。

## 边界与剩余事项

盘前或午间行情超过10分钟将拒绝实时风控评估，由既有任务写入暂停买入；需后续任务在行情恢复后重新评估，非行情一恢复立即自动解除。盘前估值策略尚需完善。

账户页面及其他调用方仍可能显示成本价补值；本轮只保证portfolio_risk拒绝无效估值，尚未全面改造净值快照与展示。分股票因子覆盖率、月度胜率口径、独立样本外证据、财务板块新鲜度、风控状态过期和写入失败保护仍待完善。不能据测试断言过拟合已解决。此前风险调度测试容器退出错误本轮未重测。

未提交、推送或主动重启；源码挂载共享，未验证全部运行进程的加载状态。
