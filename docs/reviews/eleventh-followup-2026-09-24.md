# 第十一轮追加审查（2026-09-24）

基于aaed844继续审查。

## 修复

- 财务采集与板块归属更新时间转换到上海时区后再次核对asof日期，阻止UTC同日实际为上海次日的数据通过历史日期硬闸。
- satellite_halt_today第二次读取状态时重新验证日期、严格布尔halt和normal等级，避免第一次读取正常、第二次已停买或失效时仍放行。仍未消除多次读取之间的竞态。
- 独立回测对照组合拒绝非有限收益，避免未选中股票的无穷收益污染基准和超额指标。

## 验证

容器内strategy_backtest_integrity 6、selection_gate 13、portfolio_risk_integrity 9、buy_atomicity 42、execution_gate 11、validation_hardening 20、nav_rebuild_integrity 2，共103项通过且正常退出。语法检查和git diff --check通过，测试使用隔离数据。

## 剩余问题

实时回撤仍直接用当前总资产除以历史基准资产，未扣除基准之后的外部出入金，入金可能掩盖亏损、出金可能产生虚假回撤；修复需统一基准时间和资金流水快照，不能只按当天求和避免重复扣除。历史净值账户来源隔离与非交易日出入金问题仍在。

回测月度胜率口径、基准缺失值样本选择、逐股票因子覆盖及独立样本外证据未完成。历史asof只提供日期级边界，不代表完整历史数据版本或日内PIT。选股质量/新鲜度和风控状态过期、文件失败处理尚未闭环。

未提交、推送、主动重启，未手工交易或重建生产净值。共享挂载下未验证所有运行进程加载状态。
