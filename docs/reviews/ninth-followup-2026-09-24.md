# 第九轮追加审查（2026-09-24）

保留第七、八轮未提交修改，继续检查。

## 修复

- 净值快照写入前要求账户估值明确有效，并校验总资产、现金、市值有限且总资产大于零、市值非负；拒绝缺失行情成本补值污染新的净值记录，不改写历史数据。
- 成交前目标仓位原始值必须有限且在[0,1]。此前min(正常目标, NaN)会保留正常目标，使损坏的风控参数被忽略；现在明确拦截。
- 独立策略回测的top_n、hold_days必须为正整数，拒绝负值、零、布尔值、小数、NaN及字符串，避免负切片和无效持有周期进入评估。

## 验证

容器内selection_gate 12、strategy_backtest_integrity 4、portfolio_risk_integrity 8、buy_atomicity 42、execution_gate 11、validation_hardening 20，共97项通过且正常退出。语法编译检查及git diff --check通过。测试使用临时数据库或模拟数据，未手工执行生产交易。

## 审查边界

自动选股硬闸本轮回归通过，不代表完整财务/板块质量规则或历史数据时效校验已完成。过拟合治理仍缺完整独立证据链；月度胜率口径、逐股票因子覆盖率未解决。

净值快照未使用10分钟盘中时效规则，因为任务在收盘后运行；当日收盘行情的明确来源和日期要求仍待完善。历史净值重建存在缺价持仓漏算、出入金日期及账户来源隔离问题，本轮未运行或修改重建历史。风控文件日内过期、写入失败及二次读取一致性仍待修复。此前风险调度测试退出错误本轮未复测。

本轮未提交、推送或主动重启。源码共享挂载，运行进程加载状态未统一验证。
