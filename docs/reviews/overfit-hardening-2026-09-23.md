# 过拟合约束修复记录（2026-09-23）

## 已实现

1. 修复 walk_forward 的测试段重复跳过训练窗错误；三折验证在测试段之前训练，标签边界提前一个交易日。禁止持有期重叠。自动出包及重验接入三折检查；测试不足不能回退到选择集冒充独立测试。
2. 常规、放宽收益与事件闸门加入 HAC p 值乘累计试验数校正。原噪声地板仍作诊断。新增 research_trials 保存候选搜索与 CV 次数，统计账本异常拒绝通过。旧试验只能以现有注册表/测试哈希估算，保守相加，不能声称还原了所有历史搜索。
3. 类型最低有效 IC 天数：量价/财务/板块/指数/资金流/支撑阻力 250，龙虎榜/盘口/爆量/事件 120；尾段至少24天。属于版本化保守工程门槛，不是收益保证。
4. 重验按每个历史调仓日期识别市场状态，逐适用状态至少8窗口且平均扣费超额为正；未知状态不能默认震荡。修复指数数据库回退查询漏掉截止日期条件。
5. 财务删除报告期+45天假设；龙虎榜和公告从实际 fetched_at 次日起可见；盘口归档新增 available_at，旧记录不伪造时间。没有历史版本的数据不能补造历史可用性。逐记录时点报告校验接口已提供，审批强制要求时点证明；完整供应商历史版本存储/报告自动生成仍需后续建设。
6. 收益闸门要求两个年度分别通过，缺样本不放行。5日收益改为非重叠抽样，修正年化和首期回撤；静态回测基准统一为均值。
7. 三折报告增加夏普、回撤、换手、成本和时间边界。历史权重不读取当前方向状态；生成评分卡与因子首次发现日期受训练截止日期限制，不使用当前相关簇选择历史因子。
8. LoopEngine 新包改为 shadow，重验不会将 shadow 自动晋级 active，保存和重验不恢复 paused/retired。审批新增 walk_forward、regime_validation、search_budget，全部绑定策略及规则版本；独立holdout至少8成熟窗口，shadow至少20成熟窗口。未生成任何真实批准。

## 验证

隔离回归：新增14项、审批7项、交易原子性35项通过；现有factor_eval 13项、cs_norm 20项通过。编译和diff空白检查通过。测试不构成生产收益验证。

## 运行与待验收

本次没有重启服务、没有全库重验、没有修改资金成交历史。容器挂载代码已更新，常驻进程加载状态未确认。只读检查确认审批文件不存在。

必须继续：固定候选版本后执行独立holdout和前向shadow；补齐数据供应商时间/修订版本与可复现报告；复核实际固定权重执行与滚动重估验证的区别；用历史时点股票池处理退市/成分变更；重估滑点、停牌、涨跌停及成交容量。当前CV是研究诊断，不能作为这些项目已经通过的证明。历史策略不因本次代码修复自动获得资格。
