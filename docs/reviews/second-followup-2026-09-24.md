# 第二轮追加审查（2026-09-24）

在上轮未提交修改基础上继续审查，未重启、未交易、未批准策略。

## 新发现并修复

1. 回测前视偏差：walk_forward/static_backtest在排名之前按未来收益存在性过滤股票。现在先按决策分数选股，再核对收益；所选股票缺失标签时报错，禁止替换成未来数据完整的其他股票。此修改不替代退市/停牌收益模型。
2. 选股范围：列表形式["all"]未按通用范围处理，修复与字符串all保持一致。
3. 午后风控漏调度：调度器只读hour/minute，忽略cron_expr，原任务实际只注册10:00。新增独立13:30任务，原任务保留10:00，兼容现有调度实现。
4. 风控失败残留放行：盘中风险计算返回ok=False时原先只输出跳过，保留旧放行状态；改写暂停买入状态。
5. 估值回退：新买入敞口校验不能用成本价代替缺失持仓报价；任一已持有股票报价过期或缺失均拒绝新买入，不改动卖出路径。

## 验证

validation_hardening 17、selection_gate 6、buy_atomicity 37、execution_gate 9、factor_eval 13、risk_schedule 2，合计84项通过。调度测试隔离执行函数/检查注册定义，不启动实际调度器。修改模块编译和diff检查通过。

## 保留风险

未来数据缺失会使部分回测中止，应补充数据及不可交易/退市处理，不能跳过失败结果审批。其它组合回放/独立策略回测入口仍需同口径专项梳理。盘中风控仍非连续实时评估，日内超时与异常抛出统一处理尚待完善。完整财务板块质量、供应商历史时点、固定权重与滚动权重一致性、审批原始报告核验依然未完成。

运行状态：本轮改动留在工作区，未提交；新增午后任务需运行进程加载代码并重新应用调度配置才能生效。
