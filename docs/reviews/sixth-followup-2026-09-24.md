# 第六轮追加审查（2026-09-24）

## 修复

1. 独立策略回测拒绝全空、包含无穷值的因子；选中股票未来收益必须有限，选股日期缺失收益时不再跳过。避免残缺数据生成成功报告。
2. selection_gate 查询改为 SQLite mode=ro、2 秒连接等待，并通过 closing 显式关闭。缺少数据库或表继续拒绝放行，不创建数据库或执行 schema 初始化。
3. 早盘及盘中风控将结果结构校验和 _apply_account_risk 纳入异常处理，处理异常时尝试写入红色暂停买入状态。覆盖成功响应缺字段及后续处理异常，不再仅捕获 portfolio_risk 调用异常。

## 验证

容器内 validation_hardening 20、execution_gate 11、buy_atomicity 39、selection_gate 10、strategy_backtest_integrity 2，共82项通过且进程正常退出。risk_schedule 4项在宿主Python通过且正常退出；容器内4项断言均通过，但进程退出时可重复触发 none_dealloc 引用计数错误（退出码134），尚未定位，不能称为容器全套通过。语法编译检查与git diff --check通过。

## 剩余边界

未完成真实历史全量回测或证明过拟合已消除。报告原文验证、研究与执行权重一致性、财务/板块新鲜度与质量阈值仍待完善。library策略读取仍有初始化逻辑；多次选股查询未使用统一事务快照。风控写入本身失败、日内过期及辅助建议处理失败后的精细化降级尚需后续设计。此次不改变风控调度频率，不自动执行减仓。

未提交、未推送、未重启，未进行生产交易；保留前几轮工作区修改。
